# TODO: add script bin/create_archives.py
[project]
name = "glacier-backup"
version = "0.2b2"
description = "Glacier backup package"
requires-python = ">=3.13"
dependencies = [
    "python-gnupg>=0.5.4",
]
